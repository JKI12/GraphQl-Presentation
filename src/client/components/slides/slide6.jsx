import React from 'react';

export default (props) => {
  return (
    <div className="slide slide-six">
      <div className="content">
        <h2>Mutations</h2>
        <p>As the names suggests are GraphQls way of mutation data, kinda like a POST, PUT all rolled into one</p>
        <p>This allows you to manipulate and add data to your database</p>
        <p>GraphQl is built upon this concept of writing a schema which defines the APIs types (i.e Topic), its models and what mutations, queries and other functions can be ran</p>
        <p>This is a lot to take in so instead I will show you</p>
        <img src="https://media.giphy.com/media/l41lPwlfXpBVkKcKY/giphy.gif" />
      </div>
    </div>
  );
};  